# Список всех инструментов, доступных AI.
# Каждый инструмент — словарь в формате OpenAI tool-calling.

EXECUTE_1C_QUERY: dict = {
    "type": "function",
    "function": {
        "name": "execute_1c_query",
        "description": (
            "Выполняет запрос на языке запросов 1С к подключённой базе данных 1С УНФ 3.0. "
            "Используй этот инструмент когда для ответа на вопрос пользователя нужны актуальные данные: "
            "продажи, остатки товаров, задолженности, финансовые показатели и т.п. "
            "Возвращает данные в формате JSON."
        ),
        "parameters": {
            "type": "object",
            "properties": {
                "query_text": {
                    "type": "string",
                    "description": (
                        "Текст запроса на языке запросов 1С. "
                        "Пример: 'ВЫБРАТЬ Номенклатура, КоличествоОстаток "
                        "ИЗ РегистрНакопления.ТоварыНаСкладах.Остатки()'"
                    ),
                }
            },
            "required": ["query_text"],
        },
    },
}

# Все инструменты одним списком — передаётся в chat.completions.create(tools=ALL_TOOLS)
ALL_TOOLS: list[dict] = [EXECUTE_1C_QUERY]
